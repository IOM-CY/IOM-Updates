# Work Updates Display

A lightweight browser-based app for displaying work updates on a shared display. Includes a simple admin panel to add, edit, and delete updates — all stored locally in the browser.

---

## 🚀 Quick Start

1. **Download the file**
   - Get `work-updates-display.html` from this repository.

2. **Rename it**
   - Rename the file to **`index.html`** (GitHub Pages requires this name).

3. **Enable GitHub Pages**
   1. Go to your repository **Settings → Pages**
   2. Under **Source**, select `Deploy from a branch`
   3. Choose `main` branch and `/ (root)` folder
   4. Click **Save**

4. Wait a few seconds — then open your live site:
   ```
   https://yourusername.github.io/work-updates-display/
   ```

---

## 🧩 Features
- Clean, light-mode display with timestamps and tags.
- Simple admin area (default password: `admin123`).
- Add, edit, delete, and reorder updates.
- Data saved locally (no backend required).
- Fullscreen “Kiosk Mode” for wall displays.

---

## 🔑 Admin Login
Default password: `admin123`

To change it:
1. Log in to the admin panel
2. Scroll down to **Change Admin Password**
3. Enter a new password and click **Change**

Password is stored locally in your browser (safe for local use).

---

## ⚙️ Tips
- Open in **Chrome/Edge fullscreen** for best results (F11).
- Each browser/device has its own saved updates (via `localStorage`).
- For multiple displays, simply open the hosted link on each screen.

---

## 🧑‍💻 Customization
If you’d like to:
- Add your logo or company colors
- Sync updates between multiple users
- Add animations or themes

...you can easily extend the HTML or convert it to a React app.

---

## 📜 License
Free to use and modify for internal company purposes.
